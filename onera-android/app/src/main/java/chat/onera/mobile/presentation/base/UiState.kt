package chat.onera.mobile.presentation.base

/**
 * Base marker interface for all UI states in MVI pattern.
 * Each feature should define its own sealed class/data class implementing this interface.
 */
interface UiState
