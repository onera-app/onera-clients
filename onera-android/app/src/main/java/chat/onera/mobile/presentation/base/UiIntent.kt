package chat.onera.mobile.presentation.base

/**
 * Base marker interface for all user intents/actions in MVI pattern.
 * Each feature should define its own sealed class implementing this interface.
 */
interface UiIntent
