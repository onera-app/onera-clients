package chat.onera.mobile.presentation.base

/**
 * Base marker interface for one-time UI effects (navigation, toasts, etc.)
 * Effects are consumed once and don't persist in UI state.
 */
interface UiEffect
